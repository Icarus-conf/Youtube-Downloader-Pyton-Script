version: '3.8'

services:
  web:
    build: .
    container_name: youtube-downloader
    ports:
      - "5001:5001"
    volumes:
      - ./downloads:/app/downloads
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
